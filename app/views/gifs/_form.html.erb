<%= form_for @gif, :html => { :multipart => true } do |f| %>
  <% if @gif.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gif.errors.count, "error") %> prohibited this gif from being saved:</h2>

      <ul>
      <% @gif.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
    
    <%= f.file_field :image %>
  	<p>
  		<%= f.label :tag_names %>
  		<%= f.text_field :tag_names %>
  	</p>
  	
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
