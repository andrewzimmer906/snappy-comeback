<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @gif.title %>
</p>

<%= image_tag @gif.image.url %>
<%= image_tag @gif.image.url(:medium) %>
<%= image_tag @gif.image.url(:thumb) %>

<%= link_to 'Edit', edit_gif_path(@gif) %> |
<%= link_to 'Back', gifs_path %>
